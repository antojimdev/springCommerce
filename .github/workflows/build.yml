name: Build & Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: spring_commerce_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Run tests
      working-directory: ./api
      run: mvn clean test

    - name: Build with Maven
      working-directory: ./api
      run: mvn clean package

    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      if: always()
      # TODO: Configure coverage in PHASE 14
      # - Minimum coverage threshold
      # - Coverage reports
      # - Coverage trends

    # TODO: Add Docker image build and push in PHASE 15
    # - Build Docker image
    # - Push to container registry
    # - Security scanning

    # TODO: Add deployment step in PHASE 16-17
    # - Deploy to staging
    # - Run smoke tests
    # - Deploy to production
